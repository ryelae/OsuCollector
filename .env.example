# -------------------------------------------------------
# osu! Collection Hub — environment configuration
# -------------------------------------------------------
# Copy this file to .env.local and edit as needed.

# Optional: protect the app with a shared password.
# If not set, the app is fully open (fine for local use).
APP_PASSWORD=change_me_please

# Optional: override max upload size in bytes (default 50 MB)
# MAX_UPLOAD_BYTES=52428800
